/**
 * ${project} < ${FILE} >
 *
 * @DATE    ${DATE}
 * @VERSION ${version}
 * @AUTHOR  ${author}
 * 
 * ----------------------------------------------------------------------------
 *
 * ----------------------------------------------------------------------------
 */

/*Define("SwitchBlock", Depend("~/Cox/Extends/jQuery", "~/Cox/Extends/jQuery.Easing", "~/Cox/Extends/jQuery.Dom", "~/Cox/Net/Loader"), function(require, SwitchBlock, module)
{
    var 
        jQuery = require("jQuery"),
        Dom    = require("jQuery.Dom"),
        Loader = require("Loader")
    ;
    //Loader.LoadStyleSheet();
    SwitchBlock = Class("SwitchBlock", Extends(Cox.EventSource), function(Static, Public)
    {
        Static.Horizontal = function()
        {

        };
        
        Static.Vertical = function()
        {

        };

        Public.constructor = XFunction(jQuery, Number, Optional(Function, Static.Horizontal), Optional(Boolean, false), function(block, step, customStyle)
        {
            this.Super("constructor");
            this.dispatchEvent(
                new Cox.Event("switchBefore"),
                new Cox.Event("switchAfter")
            );
            this._step   = Math.abs(step);
            this._block  = block;
            this._easing = "swing";
        });

        Public.setAnimationEasing = XFunction(String, function(easing)
        {

        });

    });
});*/