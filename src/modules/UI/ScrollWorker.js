/**
 * ${project} < ${FILE} >
 *
 * @DATE    ${DATE}
 * @VERSION ${version}
 * @AUTHOR  ${author}
 * 
 * ----------------------------------------------------------------------------
 *
 * ----------------------------------------------------------------------------
 */
Define( "ScrollWorker", Depend("~/Cox/Extends/jQuery", "~/Cox/Extends/jQuery.Dom", "~/Cox/Tools/Queue"), function(require, ScrollWorker, module)
{
    var 
        jQuery = require("jQuery"),
        Dom    = require("Dom"),
        Queue  = require("Queue")
    ;
    ScrollWorker = Class("ScrollWorker", Extends())
});

